import IndexGrid from "@/components/IndexGrid";

// Home = the full index of projects. Clicking the name anywhere on the site
// returns here.
export default function Home() {
  return <IndexGrid />;
}
